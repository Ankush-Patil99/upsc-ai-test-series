# Database Settings
db:
  postgres_url: "postgresql://upsc_user:your_password@127.0.0.1:5432/upsc_db"

# Model Settings
models:
  mcq_llm_repo: "llama3.1:8b" # Replaced DeepSeek with local Llama 3.1 8B via Ollama
  classifier_llm_repo: "llama3.1:8b" # Replaced Qwen with local Llama 3.1 8B to save VRAM
  embedding_model: "sentence-transformers/all-MiniLM-L6-v2"  # Free HF embedding model (runs well locally)
